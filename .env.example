# Copy to .env locally; never commit real credentials.
AI_GATEWAY_API_KEY=replace-me

# Optional when using a named browser-harness connection.
BU_NAME=slither-jev-game
BU_CDP_URL=http://127.0.0.1:59328
